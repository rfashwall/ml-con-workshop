# MLOps Workshop Requirements
# Python 3.11+ recommended (3.9 works but several ML wheels prefer 3.11+)
#
# Flexible pins: workshop participants install from this file.
# For a byte-identical install, use requirements.lock (generated by
# scripts/lock-requirements.sh). Regenerate the lock when this file changes.

# ML Frameworks
transformers>=4.35.0
torch>=2.0.0
torchvision
datasets>=2.14.0
accelerate>=0.26.0

# MLOps Tools
mlflow>=3.4.0
bentoml>=1.4.0

# API and Web
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Monitoring (for Module 5)
prometheus-client>=0.19.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0

# Development
pytest>=7.4.0
black>=23.10.0

# Kubeflow Pipelines SDK (v2 API)
kfp==2.14.3
